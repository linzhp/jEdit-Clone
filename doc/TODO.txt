JEDIT TO DO

2.5pre1:

- Annoying BufferTabs flicker
- Text area scroll bar size bug
- Create macro-input snap
- Update dialog-options-general snap
- Save As: don't do disk mod check
- make HyperSearch a JFrame

Later:

- Fix dialog box focus problems
- VFS:
  - Show wait cursor only once for a set of requests
  - Abortable FTP
  - FTP VFS GUI
  - If an error occurs while saving, we shouldn't clear the dirty flag
    (VFSManager.waitForRequests() returns a boolean value. If it is false,
    _closeFile() doesn't close the buffer)
  - Write VFS docs - user and developer
  - Filter events in view to implement ESCAPE, etc
  - move InputHandler to gui package and implement key handling in View
    (but let child components handle events first!!!)

- JEditTextArea.CaretUndo: set textArea reference to null when it is removed?
- text area doesn't lose focus sometimes
- TokenMarker: MARK_PREVIOUS should mark to lastOffset not lastKeyword
- XMode:
  - better error reporting
  - regexps
- Editable toolbar
- Add macros to context menu and toolbar - hack loadMenuItem() to handle
  play-macro@... specially
- Plugin devel manual needs more work:
  - document views, buffers
  - how to write text area and gutter hilites
- Mode editor
- Buffer tabs:
  - trunicate long file names (should be configurable)
  - customizable font size?
- Threaded HyperSearch
- set dirty to false when all undos undone
- Background mode

Later:

- New document model
- Folding
- Word wrap
- Reverse search
- i18n support - bidi, input methods, localizable GUI, etc
- Maybe cursor beyond EOL?
- XML-driven auto indent?

When we switch to Java 2:
- drag and drop
- Use collections
- Enhanced printing
