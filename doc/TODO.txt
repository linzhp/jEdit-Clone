TO DO

- expand fold: scroll to last line of fold
- CVS: add PluginListDownloadProgress.java
- web start: macros
- scrollToCaret() doesn't always work
- caret event not always fired
- XThis: don't throw exceptions for non-existent methods
- C+ENTER: display prompt
- need to track down s.end < s.start cases

- scroll lock key toggles 'scroll mode'
- console shell: use StreamTokenizer
- on OS X, make macos l&f the default
- folding paint up bug
- prev line fold gutter bug
- plugin manager:
  - a combined roster confirm/progress dialog box
  - plugin update
  - plugin install dialog
- enter:
  {
	foo
  }
  Collapse { line, put caret at start, delete line, undo.
- tabbing to text area fails
- rewritten folding: Fold object, pointer to next of curr. level, each
  text area has visibility hashtable
- explicit folding
- fold browser
- zlib decompression errors in plugin manager
- when loading a plugin, check if all the jars are present?
- only exit delegates at the top level (eg <?php echo "<?xml ...?>" ?>
  in PHP)
- regexps in XMode
- improve handling of DIGIT sequences
- DELEGATE in EOL_SPAN
- encoding option in VFS browser
- catch MalformedInputException and display a better error message
- failed saves should not clear the dirty flag
- reverse regexp search
- finish persistent splits
- see if new beanshell fixes trailing \n bug
- get rid of final blank page of user's guide
- update macro guide for misc. API changes:
  - markers
  - mention registers?
  - s&r changes (removal of showSearchDialog(), beanshell replace)
  - new select API
- Include John's upcoming plugin guide
- improved rectangular selections
- a 'reinsertOnNextLine' regexp for word wrap?
- aborts not always fired
- fix dragStart handling in DockableWindowContainer.TabbedPane
- C+i in shortcuts editor
