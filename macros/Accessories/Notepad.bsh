/**
 * Implements a simple notepad.
 */

import java.awt.event.*;
import java.awt.*;
import javax.swing.*;

tabs = new JTabbedPane();
for(int i = 1; i <= 5; i++)
{
	tabs.addTab("Page " + i,new JScrollPane(new JTextArea(10,30)));
}
frame(tabs).setTitle("Notepad");
